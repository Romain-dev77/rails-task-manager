
<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task) %> ==> <%= link_to "edit a task", edit_path(task.id) %>
<%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</li>

<% end %>

<%= link_to "add a task", tasks_new_path %>
